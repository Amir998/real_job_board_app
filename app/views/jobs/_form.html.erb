<%= simple_form_for(@job) do |f| %>
<% if job.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

  <ul>
    <% job.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="container form-job">
  <form>
    <div class="form-row">
      <h3>First, tell us about the position</h3>
      <div class="form-group col-md-12">
        <%= f.label "Job Title" %>
        <%= f.text_field :title, class: "form-control fch" %>
      </div>

      <div class="form-group col-md-6">
        <%= f.label "Category" %><br>
        <%= f.select :category, Job::CATEGORY, class: "custom-select" %>
      </div>

      <div class="form-group col-md-6">
        <%= f.label "Job Type" %><br>
        <%= f.radio_button :job_type, true, class: "custom-radio" %>
        <%= f.label :job_type, Job::JOB_TYPE[0], :value => "true", class: "cw"  %>
        <%= f.radio_button :job_type, false, :checked => true, class: "custom-radio" %>
        <%= f.label :job_type,  Job::JOB_TYPE[1], :value => "false", class: "cw" %>
      </div>

      <div class="form-group col-md-6">
        <%= f.label "Headquarters" %>
        <%= f.text_field :headquarters, class: "form-control fch fcw" %>
      </div>

      <div class="form-group col-md-6">
        <%= f.label "Region" %>
        <%= f.text_field :region, class: "form-control fch fcw" %>
      </div>

      <div class="form-group col-md-12">
        <%= f.label "Apply Link" %>
        <%= f.text_field :apply_link, class: "form-control fch" %>
      </div>

      <div class="form-group col-md-12">
        <%= f.label "Job Description" %>
        <%= f.text_area :description, class: "form-control fch" %>
      </div>

      <h3>Tell us more about your company</h3>
      <%= f.fields_for :companies do |builder| %>
      <%= render 'company', f: builder %>
      <% end %>

      <div class="actions">
        <%= f.submit %>
      </div>
    </div>
  </form>
</div>
<% end %>
